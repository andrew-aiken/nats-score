# Override the API/auth server (defaults to the page's own origin — the Go
# server normally serves /, /api, and /auth from one origin).
# VITE_API_BASE_URL=http://localhost:3000

# Override the NATS websocket URL (defaults to <page hostname>:8080).
# VITE_NATS_URL=ws://localhost:8080
